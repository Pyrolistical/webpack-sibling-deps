{
  "name": "lib",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist/ node_modules/",
    "install:sdk": "cp -r ../sdk node_modules/ && rm -rf node_modules/sdk/node_modules",
    "prebuild": "npm run clean && npm install && npm run install:sdk",
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "4.5.5"
  }
}
