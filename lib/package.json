{
  "name": "@pyrolistical/webpack-sibling-deps-lib",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist/ node_modules/",
    "prebuild": "npm run clean && npm install",
    "build": "tsc"
  },
  "devDependencies": {
    "@pyrolistical/webpack-sibling-deps-sdk": "0.0.0",
    "typescript": "4.5.5"
  }
}
